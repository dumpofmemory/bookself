type User {
  id: ID! @id
  uid: String
  name: String
  email: String
  photoURL: String
  isNewUser: Boolean
  books: [Book!]
}

type Book {
  id: ID! @id
  title: String!
  published: Boolean! @default(value: false)
  author: User @relation(link: INLINE)
}
