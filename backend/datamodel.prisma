type User {
  id: ID! @id
  uid: String! @unique
  name: String
  email: String
  photoURL: String
  isNewUser: Boolean
  books: [Book!]
}

type Book {
  id: ID! @id
  authors: String
  description: String
  publishedDate: String
  title: String
  ownedBy: User! @relation(link: INLINE)
}
